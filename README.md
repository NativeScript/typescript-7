## NativeScript with TypeScript 7

### Using Plain XML

`ns-xml` is a NativeScript project that demonstrates the usage of TypeScript 7 using plain XML for UI definitions.

TypeScript 6 `~17.117 s`

TypeScript 7 `~1421 ms`